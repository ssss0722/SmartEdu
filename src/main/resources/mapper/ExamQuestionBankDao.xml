<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ExamQuestionBankDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.ExamQuestionBankEntity" id="examquestionbankMap">
        <result property="title" column="title"/>
        <result property="options" column="options"/>
        <result property="score" column="score"/>
        <result property="answer" column="answer"/>
        <result property="analysis" column="analysis"/>
        <result property="type" column="type"/>
        <result property="sequence" column="sequence"/>
        <result property="tUsername" column="t_username"/>
		<result property="courseId" column="course_id"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.ExamQuestionbankVO" >
		SELECT * FROM question_bank  question_bank
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.ExamQuestionbankVO" >
		SELECT  question_bank.* FROM question_bank  question_bank
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.ExamQuestionBankView" >

		SELECT  question_bank.* FROM question_bank  question_bank
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.ExamQuestionBankView" >
		SELECT * FROM question_bank  question_bank <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	


</mapper>
