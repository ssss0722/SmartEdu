<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ScreenVideoDao">

    <select id="selectListView" resultType="com.entity.view.ScreenVideoView">
        SELECT
        sv.*,
        ut.t_name
        FROM screen_video sv
        LEFT JOIN user_teacher ut ON sv.t_username = ut.t_username
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>
    <select id="selectView" resultType="com.entity.view.ScreenVideoView">
        SELECT
        sv.*,
        ut.t_name
        FROM screen_video sv
        LEFT JOIN user_teacher ut ON sv.t_username = ut.t_username
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>
</mapper>