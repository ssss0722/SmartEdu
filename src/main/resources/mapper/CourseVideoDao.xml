<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.CourseVideoDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.CourseVideoEntity" id="courseVideoMap">
        <result property="title" column="title"/>
        <result property="course" column="course"/>
        <result property="cover" column="cover"/>
        <result property="tUsername" column="t_username"/>
        <result property="video" column="video"/>
        <result property="publishedAt" column="published_at"/>
        <result property="intro" column="intro"/>
        <result property="clicktime" column="clicktime"/>
        <result property="clicknum" column="clicknum"/>
        <result property="discussnum" column="discussnum"/>
        <result property="storeupnum" column="storeupnum"/>
    </resultMap>

	<resultMap id="courseVideoViewMap" type="com.entity.view.CourseVideoView" extends="courseVideoMap">
		<result property="tName" column="t_name"/>
	</resultMap>

	<select id="selectListVO" resultType="com.entity.vo.CourseVideoVO">
		SELECT
		cv.id,
		cv.title,
		cv.course,
		cv.cover,
		cv.t_username AS tUsername,
		ut.t_name AS tName,
		cv.video,
		cv.published_at AS publishedAt,
		cv.intro,
		cv.clicktime,
		cv.clicknum,
		cv.discussnum,
		cv.storeupnum
		FROM course_video cv
		LEFT JOIN user_teacher ut ON cv.t_username = ut.t_username
		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

	<select id="selectVO" resultType="com.entity.vo.CourseVideoVO">
		SELECT
		cv.id,
		cv.title,
		cv.course,
		cv.cover,
		cv.t_username AS tUsername,
		ut.t_name AS tName,
		cv.video,
		cv.published_at AS publishedAt,
		cv.intro,
		cv.clicktime,
		cv.clicknum,
		cv.discussnum,
		cv.storeupnum
		FROM course_video cv
		LEFT JOIN user_teacher ut ON cv.t_username = ut.t_username
		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

	<select id="selectListView" resultMap="courseVideoViewMap">
		SELECT
		cv.*,
		ut.t_name
		FROM course_video cv
		LEFT JOIN user_teacher ut ON cv.t_username = ut.t_username
		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

	<select id="selectView" resultMap="courseVideoViewMap">
		SELECT
		cv.*,
		ut.t_name
		FROM course_video cv
		LEFT JOIN user_teacher ut ON cv.t_username = ut.t_username
		<where> 1=1 ${ew.sqlSegment}</where>
	</select>




</mapper>
