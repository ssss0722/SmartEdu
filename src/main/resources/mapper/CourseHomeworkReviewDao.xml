<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.CourseHomeworkReviewDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.CourseHomeworkReviewEntity" id="zuoyepigaiMap">
        <result property="course" column="course"/>
        <result property="homeworkId" column="homework_id"/>
        <result property="tUsername" column="t_username"/>
        <result property="sUsername" column="s_username"/>
        <result property="grade" column="grade"/>
        <result property="reviewedAt" column="reviewed_at"/>
        <result property="teacherComment" column="teacher_comment"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.CourseHomeworkReviewVO" >
        SELECT
        chr.*,
        ch.homework AS homework,
        ut.t_name AS tName,
        us.s_name AS sName
        FROM course_homework_review chr
        LEFT JOIN course_homework ch ON chr.homework_id = ch.id
        LEFT JOIN user_teacher ut ON chr.t_username = ut.t_username
        LEFT JOIN user_student us ON chr.s_username = us.s_username
        <where>
            1=1
            ${ew.sqlSegment}
        </where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.CourseHomeworkReviewVO" >
        SELECT
        chr.*,
        ch.homework AS homework,
        ut.t_name AS tName,
        us.s_name AS sName
        FROM course_homework_review chr
        LEFT JOIN course_homework ch ON chr.homework_id = ch.id
        LEFT JOIN user_teacher ut ON chr.t_username = ut.t_username
        LEFT JOIN user_student us ON chr.s_username = us.s_username
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.CourseHomeworkReviewView" >
        SELECT
        chr.*,
        ch.homework AS homework,
        ut.t_name AS tName,
        us.s_name AS sName
        FROM course_homework_review chr
        LEFT JOIN course_homework ch ON chr.homework_id = ch.id
        LEFT JOIN user_teacher ut ON chr.t_username = ut.t_username
        LEFT JOIN user_student us ON chr.s_username = us.s_username
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.CourseHomeworkReviewView" >
        SELECT
        chr.*,
        ch.homework AS homework,
        ut.t_name AS tName,
        us.s_name AS sName
        FROM course_homework_review chr
        LEFT JOIN course_homework ch ON chr.homework_id = ch.id
        LEFT JOIN user_teacher ut ON chr.t_username = ut.t_username
        LEFT JOIN user_student us ON chr.s_username = us.s_username
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

    <select id="selectValue" resultType="map" >
        SELECT ${params.xColumn}, ROUND(sum(${params.yColumn}),1) total
        FROM course_homework_review
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.xColumn}
    </select>

    <select id="selectTimeStatValue" resultType="map" >
        <if test='params.timeStatType == "日"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m-%d') ${params.xColumn},
            sum(${params.yColumn}) total
            FROM course_homework_review
            <where> 1=1 ${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m-%d')
        </if>
        <if test='params.timeStatType == "月"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn},
            sum(${params.yColumn}) total
            FROM course_homework_review
            <where> 1=1 ${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        <if test='params.timeStatType == "年"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y') ${params.xColumn},
            sum(${params.yColumn}) total
            FROM course_homework_review
            <where> 1=1 ${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y')
        </if>
    </select>

    <select id="selectGroup" resultType="map" >
        SELECT ${params.column}, count(1) total
        FROM course_homework_review
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.column}
    </select>




</mapper>
