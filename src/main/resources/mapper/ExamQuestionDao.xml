<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ExamQuestionDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.ExamQuestionEntity" id="examquestionMap">
        <result property="paperid" column="paperid"/>
        <result property="questionId" column="question_id"/>
        <result property="tUsername" column="t_username"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.ExamQuestionVO" >
		SELECT * FROM exam_question  exam_question
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.ExamQuestionVO" >
		SELECT  exam_question.* FROM exam_question  exam_question
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.ExamQuestionView" >

		SELECT  exam_question.* FROM exam_question  exam_question
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.ExamQuestionView" >
		SELECT * FROM exam_question  exam_question <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	


</mapper>
